SELECT 
	A.*,
	ROUND(A.COUNT#*B.AVG#/1024/1024) LOG_MB 
FROM (
	SELECT 
		TO_CHAR(FIRST_TIME,'DD.MM.YYYY') DAY, 
		COUNT(1) COUNT# 
	FROM
		V$LOG_HISTORY 
	GROUP BY 
		TO_CHAR(FIRST_TIME,'DD.MM.YYYY') 
		ORDER BY 1 DESC
	) A,
	(
	SELECT 
		AVG(BYTES) AVG#,
		COUNT(1) COUNT# 
	FROM 
		V$LOG
	) B 
/
